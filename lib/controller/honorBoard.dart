
// useless for this app
class HonorBoardNotifier {
  // late Future<List<HonorBoardResModel>> dcHonorBoard;
  // late Future<List<HonorBoardResModel>> ndcHonorBoard;

  // Fetch and filter data for DC and NDC
  // void fetchHonorBoards() {
  //   final honorBoardFuture = HonorBoardHelper.getDCHonor();
  //
  //   dcHonorBoard = honorBoardFuture.then((list) =>
  //       list.where((item) => item.designationType == 'DC').toList());
  //
  //   ndcHonorBoard = honorBoardFuture.then((list) =>
  //       list.where((item) => item.designationType == 'NDC').toList());
  // }
}
